#!/usr/bin/env bash
set -Eeuo pipefail
# Shared binary, isolated Controller process/home/identity.
exec /opt/zerotier/zerotier-one -p"${CONTROLLER_ZT_PORT:-9993}" "${CONTROLLER_HOME:-/data/controller/one}"
