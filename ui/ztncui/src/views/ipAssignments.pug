//-
  ztncui - ZeroTier network controller UI
  Copyright (C) 2017-2021  Key Networks (https://key-networks.com)
  Licensed under GPLv3 - see LICENSE for details.

extends network_layout

block net_content
  if errors
    .alert.alert-warning
      strong 请检查下方错误

  form(method='POST' action='')
    .info-list
      .info-line
        strong 成员名称
        span= member.name
      .info-line
        strong ZeroTier 地址
        span.mono-text= member.id

    .section-heading
      div
        h3 IP 分配
        p.muted 管理此成员的受管 IP 地址

    .table-responsive
      table.table.table-striped.table-hover
        thead
          tr
            th IP 地址
            th 操作
        tbody
          each ipAssignment, index in member.ipAssignments
            tr
              td.mono-text= ipAssignment
              td
                a.btn.btn-link.btn-sm.btn-link-danger(role='button' href='/controller/network/' + network.nwid + '/member/' + member.id + '/ipAssignments/' + index + '/delete')
                  span.glyphicon.glyphicon-trash.nav-icon
                  | 删除
          tr
            td
              input#ipAddress.form-control(type='text' name='ipAddress' placeholder='输入 IP 地址' value=(undefined===ipAssignment? '' : ipAssignment.ipAddress))
            td
              button.btn.btn-primary.btn-sm(type='submit')
                span.glyphicon.glyphicon-plus.nav-icon
                | 添加

    .section-heading
      div
        h3 受管路由
        p.muted 当前网络配置的可达网段
      a.btn.btn-default.btn-sm(href='/controller/network/' + network.nwid + '/routes') 管理路由

    .table-responsive
      table.table.table-striped.table-hover
        thead
          tr
            th 目标网段
            th 网关
            th 操作
        tbody
          each route in network.routes
            tr
              td.mono-text= route.target
              td.mono-text= route.via
              td
                a.btn.btn-link.btn-sm.btn-link-danger(role='button' href='/controller/network/' + network.nwid + '/routes/' + route.target + '/delete')
                  span.glyphicon.glyphicon-trash.nav-icon
                  | 删除

  if errors
    .alert.alert-warning
      ul
        for err in errors
          li!= err.msg
